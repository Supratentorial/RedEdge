<Page
    x:Class="RedEdge.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:main="using:RedEdge.Main"
    mc:Ignorable="d"
    Padding="8"
    DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=MainViewModel}">
    <Page.Resources>
      
    </Page.Resources>
    <SplitView DisplayMode="Inline" OpenPaneLength="320" IsTabStop="False">
        <SplitView.Pane>
            <ItemsControl ItemsSource="{Binding Path=NavMenuItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock>Penis</TextBlock>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </SplitView.Pane>
        <SplitView.Content>
            <TextBlock Text="{Binding Path=PageTitle}"/>
        </SplitView.Content>
    </SplitView>
</Page>
